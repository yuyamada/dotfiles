---
phase: 01-permissions-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/claude/settings.json
autonomous: true
requirements:
  - PERM-05

must_haves:
  truths:
    - "All 22 deprecated Bash(command:*) entries use space-separated Bash(command *) syntax"
    - "List:*, Read:*, Search:* entries are unchanged"
    - "No functional regression — all previously allowed commands remain allowed"
  artifacts:
    - path: "config/claude/settings.json"
      provides: "Migrated permissions allow array"
      contains: "Bash(grep *)"
  key_links:
    - from: "config/claude/settings.json"
      to: "Claude Code permission engine"
      via: "settings.json allow array read at session start"
      pattern: "Bash\\([a-z]+ \\*\\)"
---

<objective>
Migrate all 22 deprecated `:*` syntax entries in `config/claude/settings.json` to space-separated `*` syntax.

Purpose: Eliminate deprecated syntax BEFORE adding any new permissions (locked decision — RCE risk from stale colon syntax). This is the prerequisite for Plan 02.
Output: `config/claude/settings.json` with all `Bash(command:*)` entries converted to `Bash(command *)`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-permissions-baseline/01-CONTEXT.md
@.planning/phases/01-permissions-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 22 deprecated colon-wildcard entries to space-wildcard syntax</name>
  <files>config/claude/settings.json</files>
  <read_first>
    - config/claude/settings.json (current state — understand exact line positions and entry format)
    - .planning/phases/01-permissions-baseline/01-RESEARCH.md (lines 342-371 — exact migration map for all 22 entries)
  </read_first>
  <action>
In `config/claude/settings.json`, within the `permissions.allow` array, replace every `Bash(command:*)` entry with `Bash(command *)` (colon replaced by space). There are exactly 22 entries to migrate.

The exact replacements (current value -> new value):

1. `"Bash(grep:*)"` -> `"Bash(grep *)"`
2. `"Bash(ls:*)"` -> `"Bash(ls *)"`
3. `"Bash(diff:*)"` -> `"Bash(diff *)"`
4. `"Bash(find:*)"` -> `"Bash(find *)"`
5. `"Bash(gh pr view:*)"` -> `"Bash(gh pr view *)"`
6. `"Bash(gh pr diff:*)"` -> `"Bash(gh pr diff *)"`
7. `"Bash(gh pr list:*)"` -> `"Bash(gh pr list *)"`
8. `"Bash(gh issue view:*)"` -> `"Bash(gh issue view *)"`
9. `"Bash(gh issue list:*)"` -> `"Bash(gh issue list *)"`
10. `"Bash(gh repo view:*)"` -> `"Bash(gh repo view *)"`
11. `"Bash(gh release view:*)"` -> `"Bash(gh release view *)"`
12. `"Bash(gh run view:*)"` -> `"Bash(gh run view *)"`
13. `"Bash(gh workflow view:*)"` -> `"Bash(gh workflow view *)"`
14. `"Bash(gh project view:*)"` -> `"Bash(gh project view *)"`
15. `"Bash(gh project list:*)"` -> `"Bash(gh project list *)"`
16. `"Bash(gh pr checks:*)"` -> `"Bash(gh pr checks *)"`
17. `"Bash(sed:*)"` -> `"Bash(sed *)"`
18. `"Bash(git log:*)"` -> `"Bash(git log *)"`
19. `"Bash(git branch:*)"` -> `"Bash(git branch *)"`
20. `"Bash(jq:*)"` -> `"Bash(jq *)"`
21. `"Bash(wc:*)"` -> `"Bash(wc *)"`
22. `"Bash(tail:*)"` -> `"Bash(tail *)"`

DO NOT TOUCH these entries (different format, not deprecated):
- `"List:*"` (line 8)
- `"Read:*"` (line 9)
- `"Search:*"` (line 10)

Also DO NOT TOUCH:
- `"Bash(gh search *)"` (line 27 — already space-separated)
- `"Bash(~/.claude/skills/my-tasks/scripts/*)"` (line 35 — path pattern, not command wildcard)
- Any `Read(...)` entries
- Any `mcp__*` entries

After editing, validate the JSON is well-formed with `jq . config/claude/settings.json`.
  </action>
  <verify>
    <automated>grep -c 'Bash([^)]*:\*)' config/claude/settings.json | xargs -I{} test {} -eq 0 && echo "PASS: no deprecated syntax" || echo "FAIL: deprecated syntax remains"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Bash([^)]*:\*)' config/claude/settings.json` returns 0
    - `grep -c 'Bash(grep \*)' config/claude/settings.json` returns 1
    - `grep -c 'Bash(gh pr view \*)' config/claude/settings.json` returns 1
    - `grep -c 'Bash(git log \*)' config/claude/settings.json` returns 1
    - `grep -c 'List:\*' config/claude/settings.json` returns 1 (unchanged)
    - `grep -c 'Read:\*' config/claude/settings.json` returns 1 (unchanged)
    - `grep -c 'Search:\*' config/claude/settings.json` returns 1 (unchanged)
    - `jq . config/claude/settings.json` exits 0 (valid JSON)
    - `jq '.permissions.allow | length' config/claude/settings.json` returns same count as before (no entries added or removed)
  </acceptance_criteria>
  <done>All 22 Bash(command:*) entries migrated to Bash(command *) syntax. List:*, Read:*, Search:* untouched. JSON valid. Entry count unchanged.</done>
</task>

</tasks>

<verification>
- `grep -c 'Bash([^)]*:\*)' config/claude/settings.json` returns 0 (no deprecated syntax)
- `jq '.permissions.allow | length' config/claude/settings.json` returns the original count (no additions/deletions)
- `jq . config/claude/settings.json > /dev/null` exits 0 (valid JSON)
</verification>

<success_criteria>
All 22 deprecated `:*` entries replaced with space-separated ` *` syntax. Zero deprecated entries remain. `List:*`, `Read:*`, `Search:*` unchanged. JSON valid. No entries added or removed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-permissions-baseline/01-01-SUMMARY.md`
</output>

---
phase: 02-skill-auto-flags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/claude/skills/commit/SKILL.md
autonomous: true
requirements:
  - SKIL-01
must_haves:
  truths:
    - "commit --auto stages files, generates message, commits, and pushes without any confirmation prompt"
    - "commit without --auto behaves exactly as before (all 4 confirmation gates intact)"
  artifacts:
    - path: "config/claude/skills/commit/SKILL.md"
      provides: "--auto flag support for commit skill"
      contains: "If --auto"
  key_links:
    - from: "config/claude/skills/commit/SKILL.md"
      to: "GSD orchestrators"
      via: "--auto flag passed by callers"
      pattern: "If.*--auto"
---

<objective>
Add `--auto` flag support to the commit skill so it executes end-to-end without stopping for confirmation.

Purpose: GSD orchestrators and subagents need to commit without human interaction. The `--auto` flag makes this possible while preserving the existing interactive flow for direct human use.
Output: Updated `config/claude/skills/commit/SKILL.md` with conditional auto-mode behavior in all 4 steps.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-skill-auto-flags/02-CONTEXT.md
@config/claude/rules/git.md
</context>

<interfaces>
<!-- Current commit skill structure (from config/claude/skills/commit/SKILL.md) -->
<!-- Frontmatter: name, description, allowed-tools -->
<!-- Sections: Current state (shell injections), Overview, Steps 1-4 -->
<!-- Step 1: Branch check — confirmation gate: "Confirm with the user before creating" -->
<!-- Step 2: Stage files — confirmation gate: "ask the user whether to commit them together or separately" -->
<!-- Step 3: Commit — confirmation gate: "Show it to the user and confirm before committing" -->
<!-- Step 4: Push — confirmation gate: "Ask the user if they want to push" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add --auto flag documentation and conditional behavior to commit skill</name>
  <files>config/claude/skills/commit/SKILL.md</files>
  <read_first>
    - config/claude/skills/commit/SKILL.md (current full content — the file being modified)
    - .planning/phases/02-skill-auto-flags/02-CONTEXT.md (locked decisions D-01, D-02)
    - config/claude/rules/git.md (Conventional Commits format for auto-generated messages)
  </read_first>
  <action>
Edit `config/claude/skills/commit/SKILL.md` to add `--auto` flag support. Make the following specific changes:

**1. Add an "Auto mode" section immediately after the "## Overview" section (before "## Steps"):**

```markdown
## Auto mode

If `--auto` was passed, skip all confirmation gates and proceed autonomously:

- **Branch**: If on the default branch, auto-create a feature branch from the work context (e.g. `feat/add-statusline`). If already on a feature branch, proceed on it. Do not ask.
- **Staging**: Stage only files clearly related to the current task. Exclude unrelated files silently — no error, no prompt (per D-01).
- **Commit message**: Generate a Conventional Commits message explaining *why*. Commit immediately without showing it for review.
- **Push**: Always push with `git push -u origin <branch>`. Do not ask.

When `--auto` is NOT passed, follow the interactive steps below unchanged.
```

**2. Add conditional auto-mode behavior to each step by inserting a single line at the top of each step body:**

Step 1 — insert after the `### Step 1: Check the branch` heading, before the existing "If on the default branch:" line:
```markdown
> **Auto mode**: If on the default branch, auto-create a branch from the work context (e.g. `feat/add-statusline`). If already on a feature branch, proceed. Skip to Step 2.
```

Step 2 — insert after the `### Step 2: Stage relevant files` heading, before the existing "Run `git diff`" line:
```markdown
> **Auto mode**: Stage only files related to the current task. Exclude unrelated files silently. Skip to Step 3.
```

Step 3 — insert after the `### Step 3: Commit` heading, before the existing "Check recent commits" line:
```markdown
> **Auto mode**: Generate a Conventional Commits message and commit immediately. Skip to Step 4.
```

Step 4 — insert after the `### Step 4: Ask about push` heading, before the existing "Ask the user" line:
```markdown
> **Auto mode**: Push with `git push -u origin <branch>`. Done.
```

**3. Do NOT change the frontmatter (name, description, allowed-tools) or the "## Current state" section.**

**4. Do NOT remove or modify any existing interactive-mode text.** The blockquotes are additive — the existing prose below them remains exactly as-is for interactive use.
  </action>
  <verify>
    <automated>grep -c 'Auto mode' config/claude/skills/commit/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Auto mode' config/claude/skills/commit/SKILL.md` returns 5 (1 in overview section + 4 in steps)
    - `grep 'If --auto' config/claude/skills/commit/SKILL.md` matches at least once (in the Auto mode section)
    - `grep 'Confirm with the user before creating' config/claude/skills/commit/SKILL.md` still matches (interactive gate preserved)
    - `grep 'Show it to the user and confirm before committing' config/claude/skills/commit/SKILL.md` still matches (interactive gate preserved)
    - `grep 'Ask the user if they want to push' config/claude/skills/commit/SKILL.md` still matches (interactive gate preserved)
    - `grep 'Exclude unrelated files silently' config/claude/skills/commit/SKILL.md` matches (D-01 honored)
    - `grep 'auto-create a branch' config/claude/skills/commit/SKILL.md` matches (D-02 branch behavior)
    - Frontmatter `name: commit` unchanged
    - Frontmatter `allowed-tools` line unchanged
  </acceptance_criteria>
  <done>
    - commit SKILL.md contains "## Auto mode" section with full auto-mode description
    - Each of the 4 steps has a blockquote "Auto mode" line at the top
    - All existing interactive confirmation gates remain in their original form
    - D-01 (exclude unrelated files silently) is explicitly stated
    - D-02 (auto-create branch if on default) is explicitly stated
  </done>
</task>

</tasks>

<verification>
- `grep -c 'Auto mode' config/claude/skills/commit/SKILL.md` returns 5
- `grep 'Confirm with the user' config/claude/skills/commit/SKILL.md` returns match (interactive path preserved)
- `grep 'Ask the user' config/claude/skills/commit/SKILL.md` returns match (interactive path preserved)
- File still has valid YAML frontmatter (starts with `---`, has `name:`, `description:`, `allowed-tools:`)
</verification>

<success_criteria>
- SKIL-01 fully addressed: commit skill has --auto flag that skips branch check, message confirmation, and push confirmation
- Interactive behavior completely unchanged when --auto is not passed
- D-01 honored: unrelated files excluded silently in auto mode
- D-02 honored: sensible branch auto-creation strategy documented
</success_criteria>

<output>
After completion, create `.planning/phases/02-skill-auto-flags/02-01-SUMMARY.md`
</output>

---
phase: 02-skill-auto-flags
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/claude/skills/pr/SKILL.md
autonomous: true
requirements:
  - SKIL-02
must_haves:
  truths:
    - "pr --auto pushes, generates title/body, and creates a draft PR without any confirmation prompt"
    - "pr without --auto behaves exactly as before (all 3 confirmation gates intact)"
  artifacts:
    - path: "config/claude/skills/pr/SKILL.md"
      provides: "--auto flag support for pr skill"
      contains: "If --auto"
  key_links:
    - from: "config/claude/skills/pr/SKILL.md"
      to: "GSD orchestrators"
      via: "--auto flag passed by callers"
      pattern: "If.*--auto"
---

<objective>
Add `--auto` flag support to the pr skill so it creates a draft PR end-to-end without stopping for confirmation.

Purpose: GSD orchestrators and subagents need to create PRs without human interaction. The `--auto` flag makes this possible while preserving the existing interactive flow for direct human use.
Output: Updated `config/claude/skills/pr/SKILL.md` with conditional auto-mode behavior in all 5 steps.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-skill-auto-flags/02-CONTEXT.md
@config/claude/rules/git.md
</context>

<interfaces>
<!-- Current pr skill structure (from config/claude/skills/pr/SKILL.md) -->
<!-- Frontmatter: name, description, allowed-tools -->
<!-- Sections: Current state (shell injections), Overview, Steps 1-5 -->
<!-- Step 1: Check for unpushed commits — gate: "show them to the user and ask whether to push first" -->
<!-- Step 2: Find the PR format — no gate (research step) -->
<!-- Step 3: Generate and confirm — gate: "Show the full content to the user and wait for confirmation" -->
<!-- Step 4: Create — no gate (execution step) -->
<!-- Step 5: Show the link — gate: "Ask if the user wants to open it in the browser" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add --auto flag documentation and conditional behavior to pr skill</name>
  <files>config/claude/skills/pr/SKILL.md</files>
  <read_first>
    - config/claude/skills/pr/SKILL.md (current full content — the file being modified)
    - .planning/phases/02-skill-auto-flags/02-CONTEXT.md (locked decisions, gates to skip)
    - config/claude/rules/git.md (Conventional Commits format for PR titles)
  </read_first>
  <action>
Edit `config/claude/skills/pr/SKILL.md` to add `--auto` flag support. Make the following specific changes:

**1. Add an "Auto mode" section immediately after the "## Overview" section (before "## Steps"):**

```markdown
## Auto mode

If `--auto` was passed, skip all confirmation gates and proceed autonomously:

- **Push**: If there are unpushed commits, push immediately with `git push -u origin <branch>`. Do not ask.
- **PR format**: Find the template or check recent PRs exactly as in interactive mode (no gate to skip here).
- **Title and body**: Generate a Conventional Commits title (`<type>(<scope>): <description>`, lowercase, no period, under 72 chars) and body from the commit history and diff. Create the draft PR immediately without showing for review.
- **Browser**: Do not ask about opening in browser. Just display the PR URL.

When `--auto` is NOT passed, follow the interactive steps below unchanged.
```

**2. Add conditional auto-mode behavior to each step by inserting a single line at the top of each step body:**

Step 1 — insert after the `### Step 1: Check for unpushed commits` heading, before the existing "If there are unpushed commits" line:
```markdown
> **Auto mode**: If there are unpushed commits, push immediately with `git push -u origin <branch>`. Skip to Step 2.
```

Step 2 — no change needed (no confirmation gate in this step).

Step 3 — insert after the `### Step 3: Generate and confirm` heading, before the existing "Draft a PR title" line:
```markdown
> **Auto mode**: Generate the title and body, then skip directly to Step 4 (do not show for review).
```

Step 4 — no change needed (no confirmation gate in this step).

Step 5 — insert after the `### Step 5: Show the link` heading, before the existing "Display the PR URL" line:
```markdown
> **Auto mode**: Display the PR URL. Do not ask about opening in browser. Done.
```

**3. Do NOT change the frontmatter (name, description, allowed-tools) or the "## Current state" section.**

**4. Do NOT remove or modify any existing interactive-mode text.** The blockquotes are additive — the existing prose below them remains exactly as-is for interactive use.
  </action>
  <verify>
    <automated>grep -c 'Auto mode' config/claude/skills/pr/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Auto mode' config/claude/skills/pr/SKILL.md` returns 4 (1 in overview section + 3 in steps 1, 3, 5)
    - `grep 'If --auto' config/claude/skills/pr/SKILL.md` matches at least once (in the Auto mode section)
    - `grep 'ask whether' config/claude/skills/pr/SKILL.md` still matches (Step 1 interactive gate preserved)
    - `grep 'wait for confirmation' config/claude/skills/pr/SKILL.md` still matches (Step 3 interactive gate preserved)
    - `grep 'Ask if the user wants to open' config/claude/skills/pr/SKILL.md` still matches (Step 5 interactive gate preserved)
    - `grep 'Conventional Commits' config/claude/skills/pr/SKILL.md` matches (title format specified)
    - `grep 'draft' config/claude/skills/pr/SKILL.md` matches (creates draft PR, not regular)
    - Frontmatter `name: pr` unchanged
    - Frontmatter `allowed-tools` line unchanged
  </acceptance_criteria>
  <done>
    - pr SKILL.md contains "## Auto mode" section with full auto-mode description
    - Steps 1, 3, and 5 each have a blockquote "Auto mode" line at the top
    - Steps 2 and 4 are unchanged (no confirmation gates to skip)
    - All existing interactive confirmation gates remain in their original form
    - PR is always created as `--draft` in auto mode
  </done>
</task>

</tasks>

<verification>
- `grep -c 'Auto mode' config/claude/skills/pr/SKILL.md` returns 4
- `grep 'ask whether' config/claude/skills/pr/SKILL.md` returns match (interactive path preserved)
- `grep 'wait for confirmation' config/claude/skills/pr/SKILL.md` returns match (interactive path preserved)
- File still has valid YAML frontmatter (starts with `---`, has `name:`, `description:`, `allowed-tools:`)
</verification>

<success_criteria>
- SKIL-02 fully addressed: pr skill has --auto flag that skips push confirmation, content review, and browser prompt
- Interactive behavior completely unchanged when --auto is not passed
- Draft PR created automatically with Conventional Commits title format
</success_criteria>

<output>
After completion, create `.planning/phases/02-skill-auto-flags/02-02-SUMMARY.md`
</output>

# Skill Creation

- Write all skills in English
- Save new skills to `config/claude/skills/<name>/` in the relevant project's dotfiles
  and create a symlink from `~/.claude/skills/<name>`
- Skills that cannot be open-sourced should live in the company project's config,
  not in the public dotfiles repo

# Langfuse 環境変数テンプレート
#
# このファイルを ~/.claude/langfuse.env にコピーして API キーを設定する。
# (bin/langfuse-setup 実行時に自動でコピーされる)
#
# ~/.claude/langfuse.env はマシン固有のため dotfiles には含めない。
# config/zsh/langfuse.sh が起動時に source する。

# ローカル Langfuse のホスト (self-hosted)
export LANGFUSE_HOST="http://localhost:3000"

# Langfuse UI (http://localhost:3000) → プロジェクト設定 → API Keys で発行
export LANGFUSE_PUBLIC_KEY=""
export LANGFUSE_SECRET_KEY=""

# Claude Code の Stop hook が Langfuse に送信するかどうかのマスタースイッチ。
# API キーを設定したら "true" に変更する。
export TRACE_TO_LANGFUSE="false"
